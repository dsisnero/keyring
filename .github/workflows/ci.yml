name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        crystal: ["latest"]
      fail-fast: false

    runs-on: ${{ matrix.os }}
    name: Test (${{ matrix.os }})

    steps:
    - uses: actions/checkout@v4

    - name: Set up Crystal
      uses: crystal-lang/install-crystal@v1
      with:
        crystal: ${{ matrix.crystal }}

    - name: Install system libraries
      shell: bash
      run: |
        case "${{ matrix.os }}" in
          ubuntu-latest)
            sudo apt-get update
            sudo apt-get install -y libsodium-dev libsecret-1-dev libglib2.0-dev dbus-x11 gnome-keyring
            gcc -c -fPIC src/keyring/schema_shim.c -o src/keyring/schema_shim.o $(pkg-config --cflags libsecret-1)
            eval "$(dbus-launch --sh-syntax)"
            export $(gnome-keyring-daemon --start --components=secrets)
            ;;
          macos-latest)
            brew install libsodium || true
            ;;
          windows-latest)
            vcpkg install
            ;;
        esac

    - name: Install shard dependencies
      shell: bash
      env:
        LIBSODIUM_INSTALL: "0"
      run: |
        case "${{ matrix.os }}" in
          windows-latest)
            shards install --skip-postinstall
            ;;
          *)
            shards install
            ;;
        esac

    - name: Cache shards
      uses: actions/cache@v4
      with:
        path: lib
        key: ${{ runner.os }}-shards-${{ hashFiles('shard.lock') }}

    - name: Format check
      run: crystal tool format --check src spec

    - name: Run tests
      run: crystal spec

    - name: Build binary
      run: shards build --release

    - name: Determine binary name
      id: bin-name
      shell: bash
      run: |
        case "${{ matrix.os }}" in
          windows-latest)
            echo "name=keyring_cr.exe" >> $GITHUB_OUTPUT
            ;;
          *)
            echo "name=keyring_cr" >> $GITHUB_OUTPUT
            ;;
        esac

    - name: Upload binary artifact
      uses: actions/upload-artifact@v4
      with:
        name: keyring_cr-${{ matrix.os }}
        path: bin/${{ steps.bin-name.outputs.name }}
        retention-days: 7

  lint:
    runs-on: ubuntu-latest
    name: Lint

    steps:
    - uses: actions/checkout@v4

    - name: Set up Crystal
      uses: crystal-lang/install-crystal@v1
      with:
        crystal: latest

    - name: Install system libraries
      run: |
        sudo apt-get update
        sudo apt-get install -y libsodium-dev libsecret-1-dev libglib2.0-dev
        gcc -c -fPIC src/keyring/schema_shim.c -o src/keyring/schema_shim.o $(pkg-config --cflags libsecret-1)

    - name: Install shard dependencies
      env:
        LIBSODIUM_INSTALL: "0"
      run: shards install

    - name: Format check
      run: crystal tool format --check src spec

    - name: Run Ameba
      run: shards build && crystal run lib/ameba/bin/ameba.cr -- --fail-level Error

  binaries:
    needs: [test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
      fail-fast: false

    runs-on: ${{ matrix.os }}
    name: Binary (${{ matrix.os }})

    steps:
    - uses: actions/checkout@v4

    - name: Set up Crystal
      uses: crystal-lang/install-crystal@v1
      with:
        crystal: latest

    - name: Install system libraries
      shell: bash
      run: |
        case "${{ matrix.os }}" in
          ubuntu-latest)
            sudo apt-get update
            sudo apt-get install -y libsodium-dev libsecret-1-dev libglib2.0-dev dbus-x11 gnome-keyring
            gcc -c -fPIC src/keyring/schema_shim.c -o src/keyring/schema_shim.o $(pkg-config --cflags libsecret-1)
            eval "$(dbus-launch --sh-syntax)"
            export $(gnome-keyring-daemon --start --components=secrets)
            ;;
          macos-latest)
            brew install libsodium || true
            ;;
          windows-latest)
            vcpkg install
            ;;
        esac

    - name: Install shard dependencies
      shell: bash
      env:
        LIBSODIUM_INSTALL: "0"
      run: |
        case "${{ matrix.os }}" in
          windows-latest)
            shards install --skip-postinstall
            ;;
          *)
            shards install
            ;;
        esac

    - name: Build release binary
      run: shards build --release

    - name: Determine binary name
      id: bin-name-rel
      shell: bash
      run: |
        case "${{ matrix.os }}" in
          windows-latest)
            echo "name=keyring_cr.exe" >> $GITHUB_OUTPUT
            ;;
          *)
            echo "name=keyring_cr" >> $GITHUB_OUTPUT
            ;;
        esac

    - name: Upload binary artifact
      uses: actions/upload-artifact@v4
      with:
        name: keyring_cr-${{ matrix.os }}
        path: bin/${{ steps.bin-name-rel.outputs.name }}
        retention-days: 30

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [binaries]
    runs-on: ubuntu-latest
    name: Create release

    steps:
    - uses: actions/checkout@v4

    - name: Download all binaries
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: artifacts/**/*
        generate_release_notes: true
